using System;

class Program
{
    static void Main()
    {
        Console.WriteLine(Math.Sqrt(12345));        //calculates square root of 12345 via sqrt method in library class System.Math and prints to the console
                                                    //by default the number 12345 is assumed as a double type in sqrt method
    }
}

