


@(Html.Kendo().Grid<VoiceSystem.Web.Areas.Admin.Models.Ideas.IdeaViewModel>()
      .Name("grid")
      .Columns(columns =>
      {
          columns.Bound(c => c.Id);
          columns.Bound(c => c.Title);
          columns.Bound(c => c.Description);
          columns.Bound(c => c.UserIp);
          columns.Command(command => { command.Edit(); command.Destroy(); }).Width(180);
      })
      .ToolBar(toolbar =>
      {
          toolbar.Create();
      })
      .ColumnMenu()
      .Editable(editable => editable.Mode(GridEditMode.PopUp))
      .Pageable(page => page.Refresh(true))
      .Navigatable()
      .Sortable(sortable =>
      {
          sortable.SortMode(GridSortMode.MultipleColumn);
      })
      .Filterable()
      .Scrollable()
      .DataSource(dataSource => dataSource
          .Ajax()
          .Model(model => model.Id(p => p.Id))
          .Read(read => read.Action("Tracks_Read", "Ideas"))
          .Create(create => create.Action("Tracks_Create", "Ideas"))
          .Update(update => update.Action("Tracks_Update", "Ideas"))
          .Destroy(destroy => destroy.Action("Tracks_Destroy", "Ideas"))
      ).HtmlAttributes(new { style = "height: 650px" })
)