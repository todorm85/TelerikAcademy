<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:activity="com.telerik.academy.voiceshoppinglist.MainActivityFragment"
    tools:showIn="@layout/activity_main">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <LinearLayout
            android:id="@+id/linearLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:orientation="vertical">

            <Button
                android:id="@+id/btn_add_new_shopping_list"
                style="@style/MainMenuButton"
                android:layout_height="wrap_content"
                android:text="@string/add_new_shopping_list_label" />

            <Button
                android:id="@+id/btn_my_shopping_lists"
                style="@style/MainMenuButton"
                android:layout_height="wrap_content"
                android:text="@string/my_shopping_lists_label" />

            <Button
                android:id="@+id/btn_login"
                style="@style/MainMenuButton"
                android:layout_height="wrap_content"
                android:text="@string/login_label" />

            <Button
                android:id="@+id/btn_logout"
                style="@style/MainMenuButton"
                android:layout_height="wrap_content"
                android:text="@string/logout_label" />

            <Button
                android:id="@+id/btn_backup"
                style="@style/MainMenuButton"
                android:layout_height="wrap_content"
                android:text="@string/backup_label" />

            <Button
                android:id="@+id/btn_restore"
                style="@style/MainMenuButton"
                android:layout_height="wrap_content"
                android:text="@string/restore_label" />

            <Button
                android:id="@+id/btn_help"
                style="@style/MainMenuButton"
                android:layout_height="wrap_content"
                android:text="@string/help_label" />

            <Button
                android:id="@+id/btn_exit"
                style="@style/MainMenuButton"
                android:layout_height="wrap_content"
                android:text="@string/exit" />

        </LinearLayout>
    </ScrollView>

</RelativeLayout>
